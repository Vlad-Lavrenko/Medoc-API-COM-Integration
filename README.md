# Medoc API COM Integration

REST API сервіс для інтеграції з системою M.E.Doc через COM-інтерфейс. Працює як Windows Service і надає сучасний REST API з автоматичною документацією.

## 🎯 Призначення

Проєкт створений для автоматизації роботи з M.E.Doc через REST API:
- Створення та обробка документів
- Електронне підписання
- Відправка до контролюючих органів (ДПС України)
- Моніторинг стану служби

## 🏗️ Архітектура проєкту

```
Medoc-API-COM-Integration/
├── src/
│   ├── Common/                         # Спільна бібліотека
│   │   ├── Configuration/              # Система налаштувань
│   │   │   ├── SettingsManager.cs      # Менеджер читання/запису налаштувань
│   │   │   └── SettingsValidator.cs    # Валідація налаштувань
│   │   ├── Constants/                  # Константи додатку
│   │   ├── Logging/                    # Конфігурації Serilog
│   │   └── Models/                     # Спільні моделі даних
│   │       ├── ApiResponse.cs          # Стандартизована обгортка відповідей API
│   │       ├── AppSettings.cs          # Модель налаштувань служби
│   │       └── ServiceInfo.cs          # Моделі інформації про службу
│   ├── Service/                        # REST API Windows Service
│   │   ├── Endpoints/                  # Minimal API endpoints
│   │   ├── Extensions/                 # Extension методи
│   │   ├── Middleware/                 # ASP.NET Core middleware
│   │   ├── Services/                   # Бізнес-логіка
│   │   ├── appsettings.json            # Налаштування за замовчуванням
│   │   └── Program.cs                  # Точка входу
│   └── Service.Tests/                  # Unit-тести
│       └── Unit/                       # Unit тести для сервісів
└── logs/                               # Логи (генеруються автоматично)
```

## 🚀 Технологічний стек

### Платформа
- **.NET 10.0** — остання версія .NET платформи
- **C# 13** — сучасні можливості мови
- **ASP.NET Core Minimal API** — легковагий підхід до створення API

### Документація API
- **OpenAPI 3.1** — стандарт опису REST API
- **Scalar 2.12** — сучасний інтерактивний UI для документації API

### Логування
- **Serilog 4.2** — structured logging
- **Serilog.Sinks.File** — запис у текстові файли з ротацією
- **Serilog.Sinks.Console** — вивід у консоль
- **Enrichers** — автоматичне додавання контексту (machine name, thread ID, process ID)

### Конфігурація
- **JSON налаштування** — гнучка система конфігурації
- **SettingsManager** — централізоване керування налаштуваннями
- **Data Annotations** — валідація налаштувань

### Тестування
- **xUnit 2.9** — фреймворк для unit-тестів
- **NSubstitute 5.3** — mocking library
- **FluentAssertions 7.0** — виразні assertions

### Deployment
- **Windows Service** — фонове виконання служби
- **Serilog Request Logging** — автоматичне логування HTTP запитів

## 📋 Реалізований функціонал

### API Endpoints

#### `GET /health`
Перевірка стану служби

**Відповідь:**
```json
{
  "result": "ok",
  "errorMsg": "",
  "data": {
    "status": "Healthy",
    "checkedAt": "2026-02-13T00:35:00Z",
    "details": {
      "uptime": 123.45
    }
  }
}
```

#### `GET /info`
Інформація про службу (версія, середовище, хост, час запуску)

**Відповідь:**
```json
{
  "result": "ok",
  "errorMsg": "",
  "data": {
    "service": "Service",
    "version": "1.0.0.0",
    "environment": "Development",
    "hostName": "DESKTOP-PC",
    "startedAt": "2026-02-13T00:30:00Z"
  }
}
```

### Стандартизований формат відповідей

Усі endpoint'и повертають однаковий формат:

**Успішна відповідь:**
```json
{
  "result": "ok",
  "errorMsg": "",
  "data": { }
}
```

**Відповідь з помилкою:**
```json
{
  "result": "error",
  "errorMsg": "Опис помилки",
  "data": null
}
```

## ⚙️ Система налаштувань

### Файли конфігурації

Проєкт підтримує дворівневу систему налаштувань:

1. **appsettings.json** — налаштування за замовчуванням (в проєкті)
2. **C:\ProgramData\MedocIntegration\settings.json** — користувацькі налаштування

### Доступні налаштування

#### API налаштування

- **Address** — адреса для прослуховування
  - `http://localhost` — тільки локальний доступ
  - `http://0.0.0.0` — доступ з усіх мережевих інтерфейсів
- **Port** — порт сервера (1-65535)

#### Налаштування логування

- **MinimumLevel** — мінімальний рівень логування
  - `Verbose` — найдетальніше
  - `Debug` — відладочна інформація
  - `Information` — загальна інформація (за замовчуванням)
  - `Warning` — попередження
  - `Error` — тільки помилки
  - `Fatal` — критичні помилки

### Приклад коніфгурації

**appsettings.json:**
```json
{
  "AppSettings": {
    "Api": {
      "Address": "http://localhost",
      "Port": 5000
    },
    "Logging": {
      "MinimumLevel": "Information"
    },
    "UserSettingsPath": "C:\\ProgramData\\MedocIntegration\\settings.json"
  }
}
```

**C:\ProgramData\MedocIntegration\settings.json:**
```json
{
  "appSettings": {
    "api": {
      "address": "http://0.0.0.0",
      "port": 8080
    },
    "logging": {
      "minimumLevel": "Warning"
    }
  }
}
```

### Особливості

- **ProgramData розташування** — доступне для Windows Service під системними правами
- **Автоматична валідація** — перевірка коректності налаштувань при старті
- **Готовність до UI** — `SettingsManager` дозволяє Desktop UI читати/змінювати налаштування
- **Без перекомпіляції** — зміна налаштувань через JSON файл

## 🔧 Налаштування та запуск

### Вимоги
- Windows 10/11 або Windows Server 2019+
- .NET 10.0 Runtime
- Visual Studio 2022 17.13+ (для розробки)

### Запуск в режимі розробки

1. Клонуйте репозиторій:
```bash
git clone https://github.com/Vlad-Lavrenko/Medoc-API-COM-Integration.git
cd Medoc-API-COM-Integration
```

2. Відкрийте solution у Visual Studio 2022:
```
Medoc API COM Integration.slnx
```

3. Запустіть проєкт Service (F5)

4. Відкрийте Scalar UI:
```
http://localhost:5000/scalar/v1
```

### Встановлення як Windows Service

```powershell
# Публікація проєкту
dotnet publish src/Service/Service.csproj -c Release -o C:\Services\MedocAPI

# Створення служби
sc create MedocIntegrationService binPath="C:\Services\MedocAPI\Service.exe"

# Запуск служби
sc start MedocIntegrationService
```

### Зміна налаштувань

Щоб змінити порт або рівень логування:

1. Відредагуйте `C:\ProgramData\MedocIntegration\settings.json`
2. Перезапустіть службу:
```powershell
sc stop MedocIntegrationService
sc start MedocIntegrationService
```

## 📊 Логування

Логи зберігаються у директорії `logs/` з ротацією щодня:

```
logs/
├── medocservice-20260213.log
├── medocservice-20260214.log
└── ...
```

**Формат логу:**
```
2026-02-13 00:35:15.123 +02:00 [INF] [MedocIntegration.Service.Program] Starting Medoc API COM Integration - MedocService
2026-02-13 00:35:16.456 +02:00 [INF] HTTP GET /health responded 200 in 12.3456ms
```

**Особливості:**
- Автоматична ротація щодня
- Зберігання логів протягом 30 днів
- Shared access — можливість читання файлів іншими процесами
- Structured logging для легкого парсингу
- **Динамічна зміна рівня** — через файл налаштувань

## 🧪 Тестування

Запуск unit-тестів:

```bash
dotnet test src/Service.Tests/Service.Tests.csproj
```

**Покриття тестами:**
- ✅ HealthService
- ✅ InfoService
- ✅ Endpoints (через integration тести)

## 🔐 Безпека

- Глобальна обробка виключень через middleware
- Структуроване логування всіх запитів
- Стандартизовані повідомлення про помилки
- Валідація налаштувань

## 🎯 Roadmap

- [x] REST API з OpenAPI/Scalar документацією
- [x] Structured logging через Serilog
- [x] Система налаштувань (JSON)
- [ ] COM інтеграція з M.E.Doc
- [ ] Endpoints для створення документів
- [ ] Електронне підписання через ЕЦП
- [ ] Desktop UI для керування службою
- [ ] Авторизація та аутентифікація API

## 📝 Ліцензія

Приватний проєкт.

## 👨‍💻 Автор

**Vlad Lavrenko**
- GitHub: [@Vlad-Lavrenko](https://github.com/Vlad-Lavrenko)
- Email: vlad.lavrenko@gmail.com

## 🔗 Корисні посилання

- [ASP.NET Core Documentation](https://docs.microsoft.com/aspnet/core)
- [Serilog Documentation](https://serilog.net/)
- [Scalar API Documentation](https://scalar.com/)
- [M.E.Doc Official Site](https://medoc.ua/)
